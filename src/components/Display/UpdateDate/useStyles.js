import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    width: "100%",
    paddingTop: 10,
    textAlign: "center",
    fontSize: 13,
    lineHeight: "20px",
    color: "#838c88",
  }
}));