import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  root: {
    position: "absolute",
    top: 0,
    left: 0,
    width: "100%",
    paddingTop: 8,
    textAlign: "center",
    fontSize: 12,
    lineHeight: "20px",
    color: "#696F6C",
  }
}));